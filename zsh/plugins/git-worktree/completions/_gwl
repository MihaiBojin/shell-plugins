#compdef gwl
# Completion for `gwl [QUERY]`. The query is free text matched by the picker.
_arguments -s \
  '(-h --help)'{-h,--help}'[show usage]' \
  '(-l --list *)'{-l,--list}'[print the worktrees instead of picking one]' \
  '*:query:_gw_branches'
